{% extends 'base.html.twig' %}

{% block title %}Votre recherche{% endblock %}

{% block body %}

    <section class="container produit">
    <div class="row">
    {# //card-column   card-deck #}
        {% for p in Produit %}
            {{include("produit/carte.html.twig", {produit:p}) }}    

            {# autre facon de faire si on veut passer une nouvelle variable 
                 {% for l in livres %}
                {{include("livre/vignette.html.twig",{livre: l})}}  
            #}
            {% else %}
                  {{message|default("Il n'y a aucun produit correspondants ")}}
        {% endfor %}
    </div>
   </section>



{% endblock %}
